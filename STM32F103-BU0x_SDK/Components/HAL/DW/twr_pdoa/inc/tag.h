/*! ---------------------------------------------------------------------------
 * @file    node.h
 * @brief   DecaWave
 *             bare implementation layer
 *
 * @author Decawave Software
 *
 * @attention Copyright 2018 (c) DecaWave Ltd, Dublin, Ireland.
 *            All rights reserved.
 *
 */

#ifndef __NODE__H__
#define __NODE__H__ 

#ifdef __cplusplus
 extern "C" {
#endif

#include "uwb_frames.h"
#include "msg_time.h"

#include "default_config.h"	 
#include "error.h"

#define DEEP_SLEEP                      (0)				//????????




#define EVENT_BUF_SIZE                  (0x02)			//???buf????
#define FILTER_SIZE                     (10)			//???????


#define MASK_40BIT                      (0x00FFFFFFFFFFULL)  // DW1000 counter is 40 bits
#define MASK_TXDTS                      (0x00FFFFFFFE00ULL)  //The TX timestamp will snap to 8 ns resolution - mask lower 9 bits.

#define GATEWAY_ADDR                    (0x0000)		//??????(?????????)

#define TAG_BLINK_TX_RC_RX_US           (500)			//???????Blink->????RngCfg??????
#define RC_RX_TIMEOUT_US                (300)			//???????rngcfg??????
#define RX_RELAX_TIMEOUT_SY             (20)			//?????????????
#define RX_FIXED_TIMEOUT_US             (300)			//????????????


#define SPEED_OF_LIGHT                  (299702547.0)     // in m/s in the air

//-----------------------------------------------------------------------------

//-----------------------------------------------------------------------------


/* ??????????? */
typedef struct
{
	uint16_t    addr16;         //??????
	uint8_t     rangeNum;       //???????????? number from Tag Poll and Final messages, which indicates the current range number
	uint32_t    resTime_us;     //????????? reception time of the end of the Final from the Tag wrt node's SuperFrame start, microseconds
	uint16_t    mask;
	uint16_t    dist_cm[MAX_ANCHOR_LIST_SIZE];	//?????????

	float       pre_dist_m;		//?????
	float       pre_fdist_m;	//?????(???)
}result_t;



/* ??????? */
typedef struct
{
	struct{
		int16_t     txDataLen;						//???????
		uint8_t     arr[STANDARD_FRAME_SIZE];		//?????????
	};

	enum {
		Twr_Tx_Poll_Sent,							//???????Poll??
		Twr_Tx_Resp_Sent,							//?????Resp??
		Twr_Tx_Final_Sent,							//???????Final??
	}txState;
	uint8_t         is_send;						//????????????
	uint8_t         seqNum;							//????????seq
	uint8_t         txFlag;							//???????? DWT_START_TX_IMMEDIATE DWT_START_TX_DELAYED & DWT_RESPONSE_EXPECTED

	uint8_t         t_timeStamp[TS_40B_SIZE];		//????????

	uint32_t        delayedTxTimeH_sy;  			//???????(??32??) Delayed transmit time (high32)
	uint32_t        delayedRxTime_sy;				//???????
	uint16_t        delayedRxTimeout_sy;			//??????
}tx_pckt_t;


/* ??????? */
typedef struct 
{
	struct{
		int16_t     rxDataLen;						//???????
		uint8_t     arr[STANDARD_FRAME_SIZE];		//?????????
	};
	
	uint8_t         r_timeStamp[TS_40B_SIZE];		//????????
	
	uint32_t        TickTimeStamp;					//???????
}rx_pckt_t;


/* TWR???? */
typedef struct
{
	int      testAppState ;							//??
	int      done ;									//???

	// ????UWB????
	struct{
		rx_pckt_t   buf[EVENT_BUF_SIZE];
		int         head;
		int         tail;
	}rxPcktBuf;

	// ????UWB????
	struct{
		tx_pckt_t  buf;
	}txPcktBuf;

	// twr???????
	struct{
		struct{
			uint16_t     tagAddr;					//???????
			uint16_t     panID;						//???panID
			sfConfig_t   sfConfig;					//?????
		}general;

		struct{
			uint8_t  twr_sn;						//?????????
			int8_t   remainingRespToRx;
			int8_t   rxResponseMask;
			int      slotCorr_ms;					//??????
			uint16_t a2t_usercmd;					//????????????????

			struct{
				uint64 tagPollTxTime;   					//???Poll????????????
				uint64 tagRespRxTime[MAX_ANCHOR_LIST_SIZE];	//???Resp????????????
				uint64 tagFinalTxTime;  					//???Final????????????
			}twr_timestamp;
		}measure;
		
		struct{
			bool DwCanSleep;						//?????????
			uint8_t event;							//?????????
			
			bool timeron;							//??????????????
			uint32_t interval_in_ms;				//??????????,????blinkfast blinkslow????
			uint32_t nextWakeUpTime_ms;				//?????????????????????
			uint32_t preWakeUpTime_ms;				//???????????????
		}machine_period;

		struct{
			msg_time_t    poll;
			msg_time_t    response;
			msg_time_t    final;
		}msg_time;	
	}twr_tag;
}twr_tag_info_t;

enum inst_done
{
	INST_NOT_DONE_YET = 0,				//???????????????????????????
	INST_DONE_WAIT_FOR_NEXT_EVENT,		//????????????????????????
	
	INST_DONE_WAIT_FOR_NEXT_EVENT_TO	//??????????????????????????(??????????)
};



enum inst_states
{
	TA_INIT,

	/*TOF*/
	TA_TXPOLL_WAIT_SEND,
	TA_TXRESP_WAIT_SEND,
	TA_TXFINAL_WAIT_SEND,
	TA_TXSYSY_WAIT_SEND,
	TA_TXE_WAIT,

	TA_TX_WAIT_CONF,		// ????????????????
	TA_RXE_WAIT,			// ????????????
	TA_RX_WAIT_DATA,		// ??????????
	TA_SLEEP_DONE,			// ??????
	
	DWT_SIG_SLEEP_TIMEOUT	// ??????????
};






//-----------------------------------------------------------------------------
// exported functions prototypes
//
extern twr_tag_info_t * getTwrInfoPtr_tag(void);
static void tag_received_timeout_set(twr_tag_info_t *pTwrInfo, bool error);


//????????
static void prepare_twr_poll_msg(tx_pckt_t *pTxPckt, twr_tag_info_t *pTwrInfo);				//??????poll
static void prepare_twr_resp_msg(rx_pckt_t *pRxPckt, twr_tag_info_t *pTwrInfo);				//?????resp
static void prepare_twr_final_msg(tx_pckt_t *pTxPckt, twr_tag_info_t *pTwrInfo);			//??????final
static void twr_uwb_process(rx_pckt_t *pRxPckt, twr_tag_info_t *pTwrInfo);


int tagtestapprun(twr_tag_info_t *pTwrInfo);
void tag_instance_run(void);
void tag_pdoa_task(void);


extern void tag_start(void);
//-----------------------------------------------------------------------------


#ifdef __cplusplus
}
#endif

#endif /* __NODE__H__ */
